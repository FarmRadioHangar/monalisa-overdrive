# monalisa-mom-poc
This is a work in progress!

To run messaging server:

```
npm run build:live
```

To start client app:

```
cd client
npm start
```

Access the application from <http://localhost:8081>.
